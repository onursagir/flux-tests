# IMPORTANT: This is a placeholder. Do NOT commit real secrets to Git.
#
# Before deploying, create this secret manually on the cluster:
#
#   kubectl -n infisical create secret generic infisical-secrets \
#     --from-literal=ENCRYPTION_KEY=$(openssl rand -hex 16) \
#     --from-literal=AUTH_SECRET=$(openssl rand -hex 16)
#
# The Helm chart manages PostgreSQL and Redis credentials internally.
# Once Infisical is running, it will manage all other secrets for your stack.
