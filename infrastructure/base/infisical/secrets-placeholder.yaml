# IMPORTANT: This is a placeholder. Do NOT commit real secrets to Git.
#
# Before deploying, create these secrets manually on the cluster:
#
#   kubectl -n infisical create secret generic infisical-postgresql \
#     --from-literal=password=<your-postgres-password>
#
#   kubectl -n infisical create secret generic infisical-redis \
#     --from-literal=password=<your-redis-password>
#
#   kubectl -n infisical create secret generic infisical-encryption \
#     --from-literal=ENCRYPTION_KEY=$(openssl rand -hex 16) \
#     --from-literal=AUTH_SECRET=$(openssl rand -hex 16)
#
# Once Infisical is running, it will manage all other secrets for your stack.
